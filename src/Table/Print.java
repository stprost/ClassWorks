package Table;

import java.util.ArrayList;

public class Print {
    public static void prn(HT ht) {
        for (int i = 0; i < ht.getTable().length; i++){
          //  if (ht.getTable()[i] instanceof ArrayList) ArrayList temp = ht.getTable()[i];
        }
    }
}
